// Generated by scripts/check-native-copy.mjs — DO NOT EDIT.
//
// Source of truth: docs/legal/native-copy.json (ADR 0018 D9).
// Regenerate with `node scripts/check-native-copy.mjs --write`.
//
// These notices are legally registered copy backed by the control register. Editing them here
// changes what a voter is told without changing what the compliance guards read, so the build
// rejects any edit that does not come from the registry.

enum LegalCopy {
    /// Opens the ballot acknowledgement on the card, live and archived alike.
    static let numbersAreYourChoice = "These numbers are your choice."

    /// The DISC-NO-RECOMMENDATION disclaimer itself (docs/legal/required-checks.json). Registered without terminal punctuation because the live and archived notices continue the sentence differently.
    static let noRecommendation = "How2Vote does not recommend a candidate or a preference order"

    /// Distinguishes the plan from an official ballot paper on the live card.
    static let notABallotPaper = "A voting plan is not a ballot paper: copy your numbers onto the official paper at the polling place and follow the AEC's instructions."

    /// Already a single-sourced constant on the web (PREFERENCE_SOURCE_NOTICE); registered so the native rendering is bound to the same string.
    static let preferenceSource = "Preference order selected by the user."

    /// The plan foot's user-authorship statement, which carries the s321D authoriser model (ADR 0010).
    static let userAuthoredOrder = "You chose every number — How2Vote does not recommend a candidate or a preference order."

    /// Announced as part of a link's accessible name before it is followed (WCAG 3.2.5), so a screen-reader user is not dropped into an unexplained context. The wording is channel-specific: the shells open an in-app browser rather than a new tab, and link-conventions.test.ts already holds the web to this on its side.
    static let externalLinkCue = "opens in an in-app browser"

    /// States how the ×10 importance lever works and, in the same breath, its limit — that it attaches only to the strongest answers. A native review screen that described the weighting differently would be a second, unreviewed account of the matching method.
    static let importanceWeighting = "Tap a question to change your answer. Star (★) the issues that matter most — only your strongest answers can count ten times as much."

    /// The star control's own label, naming the multiplier the scoring model applies.
    static let importanceMultiplier = "Extremely important (×10)"

    /// One paragraph of the AEC Spatial Data Download derivative-product notice, which the licence requires to be DISPLAYED wherever the boundary data is shown — a bare attribution credit does not satisfy it. Registered against the source record itself, not a component, because that record is where the accepted wording lives and where the web reads it from too (apps/web/src/lib/mapLicence.ts re-exports it).
    static let mapLicenceIncorporates = "This product (how2vote.au) incorporates data that is: © Commonwealth of Australia (Australian Electoral Commission) 2026"

    /// One paragraph of the AEC Spatial Data Download derivative-product notice, which the licence requires to be DISPLAYED wherever the boundary data is shown — a bare attribution credit does not satisfy it. Registered against the source record itself, not a component, because that record is where the accepted wording lives and where the web reads it from too (apps/web/src/lib/mapLicence.ts re-exports it).
    static let mapLicencePermission = "The Data (Commonwealth Electoral Boundaries (various years)) has been used in how2vote.au with the permission of the Australian Electoral Commission."

    /// One paragraph of the AEC Spatial Data Download derivative-product notice, which the licence requires to be DISPLAYED wherever the boundary data is shown — a bare attribution credit does not satisfy it. Registered against the source record itself, not a component, because that record is where the accepted wording lives and where the web reads it from too (apps/web/src/lib/mapLicence.ts re-exports it).
    static let mapLicenceNoWarranty = "The Australian Electoral Commission has not evaluated the Data as altered and incorporated within how2vote.au, and therefore gives no warranty regarding its accuracy, completeness, currency or suitability for any particular purpose."

    /// One paragraph of the AEC Spatial Data Download derivative-product notice, which the licence requires to be DISPLAYED wherever the boundary data is shown — a bare attribution credit does not satisfy it. Registered against the source record itself, not a component, because that record is where the accepted wording lives and where the web reads it from too (apps/web/src/lib/mapLicence.ts re-exports it).
    static let mapLicencePersonalUse = "You may use how2vote.au to load, display, print and reproduce views obtained from the Data, retaining this notice, for your personal use, or use within your organisation only."

    /// What the tool does, on the first screen. The placeholder is the question count. Registered because it states what the comparison IS — the parties' recorded votes, not an opinion of them.
    static func landingLedeUpcoming(_ v1: String) -> String {
        "Answer " + String(v1) + " real questions the current Parliament has voted on and see how your views compare with the parties' recorded votes."
    }

    /// The same claim for an election that has a ballot. Placeholders are the question count and the election year.
    static func landingLedeElection(_ v1: String, _ v2: String) -> String {
        "Answer " + String(v1) + " real questions parliament has voted on and see how your views compare with the parties' recorded votes, for your " + String(v2) + " ballot — House and Senate."
    }

    /// Bounds what a provisional comparison is and is not, before a voter starts one. Without it the first screen would imply a comparison against an election that has not been called.
    static let landingProvisional = "The next federal election hasn't been announced yet, so this is a provisional comparison against the current Parliament — the questions may change, and there are no candidates or printable how-to-vote plan yet."

    /// Marks a past election as historical, so a comparison against it is not mistaken for current advice.
    static let landingArchived = "This election has already been held — what follows is a historical comparison, scored on the record as it stood then."

    /// A claim about the basis of the comparison, made on the first screen.
    static let landingTrustRecords = "Built from real parliamentary voting records"

    /// A neutrality claim, and one the methodology page has to keep true.
    static let landingTrustEvenHanded = "Even-handed — the method is public and deterministic"

    /// A privacy claim: no account, on-device, analytics off unless chosen. Registered so the native surface cannot state a weaker or stronger version of it than the web.
    static let landingTrustNoAccount = "No account, works offline, analytics off by default"
}
